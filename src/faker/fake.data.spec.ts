/*
import { FakeData } from './fake.data';
describe('FakeData', () => {
  it('should create fake data', () => {
    const fakeData = new FakeData();
    expect(fakeData.adopters.length).toBe(100);
    expect(fakeData.adoptionApplications.length).toBe(100);
    expect(fakeData.adoptionContracts.length).toBe(100);
    expect(fakeData.animals.length).toBe(100);
    expect(fakeData.enclosures.length).toBe(100);
    expect(fakeData.healthRecords.length).toBe(100);
    expect(fakeData.shelters.length).toBe(100);
  });
});
*/
describe('FakeData', () => {
  it('should create fake data', () => {});
});
